interface:
  display_name: "Suno Cli"
  short_description: "Repo-specific patterns and workflows for suno-cli"
  default_prompt: "Use the suno-cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true